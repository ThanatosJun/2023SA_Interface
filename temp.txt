<!-- <button id="Register" type="button" class="btn btn-primary btn-lg mb-3">註冊</button>
<button id="Vistor" type="button" class="btn btn-primary btn-lg mb-3">訪客瀏覽</button>
<script>
// After Press Submit button, submit data to controller and naviate to Login.html
    document.getElementById("registrationConfirm").addEventListener("click", function() {
        window.location.href = "Login.html";
    });
</script> -->

// Error but I can't solve so leave for the future. But these function is not a big deal
// birthdayInput.setCustomValidity(isValidBirthday ? "" : "Invalid birthday format");
// emailInput.setCustomValidity(isValidEmail ? "" : "Invalid email format");
// cellphoneInput.setCustomValidity(isValidCellphone ? "" : "Invalid cellphone format");


// loadBootstrap.js

// 創建一個 script 元素
var bootstrapScript = document.createElement('script');

// 設置 script 的 src 屬性為 Bootstrap 的 CDN 連結
bootstrapScript.src = 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js';

// 設置 script 的 integrity 屬性（可選，提供額外的安全性）
bootstrapScript.integrity = 'sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL';

// 設置 script 的 crossorigin 屬性
bootstrapScript.crossOrigin = 'anonymous';

// 將 script 元素附加到 HTML 的 head 元素中
document.head.appendChild(bootstrapScript);

    document.getElementById("EmergencyCYes").addEventListener("click", function() {
        // 顯示 EmergencyConfirmationModal
        $('#EmergencyConfirmationModal').modal('show');

        // 檢查道具數量是否足夠，如果不夠，顯示 UnenoughEmergencyPropsModal
        // 這裡假設你有一個函式 checkPropsSufficiency() 用來檢查道具數量
        if (!checkPropsSufficiency()) {
            $('#UnenoughEmergencyPropsModal').modal('show');
        }
    });

    // 假設這是檢查道具數量的函式，返回 true 表示足夠，false 表示不足
    function checkPropsSufficiency() {
        // 在這裡檢查道具數量，返回 true 或 false
        // 這裡只是一個示例，你需要根據你的邏輯來實現
        var propsSufficient = true;
        return propsSufficient;
    }

// 單個顏色更改
    // var Follow_Button = document.getElementById(#Follow_Button)
    // Follow_Button.addEventListener("click", function(){

    //     Method 1 法一
    //     var computedStyle = window.getComputedStyle(Follow_Button);
    //     var backgroundColor = computedStyle.backgroundColor;

    //     console.log(backgroundColor);
    //     if (backgroundColor === "rgb(0, 180, 0)") {
    //     Follow_Button.style.backgroundColor = "#D91621";
    //     } else {
    //     Follow_Button.style.backgroundColor = "#00B400";
    //     }

    //     Method 2 法二
    //     if(Follow_Button.style["backgroundColor"] === "rgb(0, 180, 0)"){
    //         Follow_Button.style.backgroundColor = "#D91621";
    //     } else{
    //         Follow_Button.style.backgroundColor = "#00B400";
    //     }
    // });

    //  Method 3 法三
    // $(document).ready(function(){
    //     $("button").click(function(){
    //         var currentColor = $("#Follow-Button").css("background-color");
        
    //         if (currentColor === "rgb(0, 180, 0)") {
    //             // 當按鈕是綠色時，轉為紅色
    //             $("#Follow-Button").css("background-color", "#D91621");
    //         } else {
    //             // 當按鈕是紅色時，轉為綠色
    //             $("#Follow-Button").css("background-color", "#00B400");
    //         }
    //     });
    // });